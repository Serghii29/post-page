import React from 'react';
import './App.scss';
import { PostsList } from './PostsList';

const App: React.FC = () => (
  <div className="container">
    <PostsList />
  </div>
);

export default App;
